AWSTemplateFormatVersion: '2010-09-09'
Description: CloudFormation template to create a Lambda functions that Aurora databases tagged with [shutdown:true] at 20:00 JST daily.

Resources:
  MyLambdaExecutionRole:
    Type: AWS::IAM::Role
    Properties: 
      AssumeRolePolicyDocument: 
        Version: 2012-10-17
        Statement: 
          - Effect: Allow
            Principal: 
              Service: lambda.amazonaws.com
            Action: sts:AssumeRole
      Policies: 
        - PolicyName: AllowLambdaToStopRDS
          PolicyDocument: 
            Version: 2012-10-17
            Statement: 
              - Effect: Allow
                Action: 
                  - logs:CreateLogGroup
                  - logs:CreateLogStream
                  - logs:PutLogEvents
                  - rds:DescribeDBInstances
                  - rds:StopDBInstance
                Resource: "*"

  StopRDSFunction:
    Type: AWS::Lambda::Function
    Properties: 
      Handler: stop_rds.handler
      Role: !GetAtt MyLambdaExecutionRole.Arn
      Code: ./src/stop_rds.py
      Runtime: python3.12
      Timeout: 300

  StopRDSRule:
    Type: AWS::Events::Rule
    Properties:
      ScheduleExpression: 'cron(0 11 * * ? *)'  # JST 20:00 is UTC 11:00
      Targets:
        - Arn: !GetAtt StopRDSFunction.Arn
          Id: StopRDSFunctionTarget

  PermissionForRDSEventsToInvokeLambda:
    Type: AWS::Lambda::Permission
    Properties:
      FunctionName: !Ref StopRDSFunction
      Action: lambda:InvokeFunction
      Principal: events.amazonaws.com